# Copy to .env (never commit .env). Create key: Cursor dashboard → service accounts / API keys.
# https://cursor.com/docs/account/enterprise/service-accounts
CURSOR_API_KEY=

# Optional, if your network uses a proxy
# HTTPS_PROXY=
# https_proxy=
